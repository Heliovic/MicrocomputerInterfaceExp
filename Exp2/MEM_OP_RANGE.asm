DATA SEGMENT
	;数据段默认起始地址为0200H
	DATA1 DW 16 DUP(1H)
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE, DS:DATA
START:
	;使用如下命令设置段地址
	MOV AX, 0A000H
	MOV DS, AX
	
	MOV AX, 0FEDCH
	MOV BX, 0H
	
	MOV [BX], AX
	
	
	
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START